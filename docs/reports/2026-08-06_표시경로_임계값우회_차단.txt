표시 경로 임계값 우회 차단 (2026-08-06)

증상: 배민 쿠폰/공지 문자에 "공식 자료를 찾았습니다" + NIA 디지털정보격차
      실태조사 URL 이 초록으로 나감(기관 불일치 오매칭).

원인(두 지점):
 1) search.py: matched_evidence(근거_검증표, min_score=1 키워드매칭, 11건 중
    10건 NIA)를 유사도 확신검증 없이 '확신 근거'로 취급 → "이용" 한 단어로
    NIA 통계가 needs_check 를 만듦.
 2) verdict.js: '못 찾았어요' 분기가 refs.length===0 을 요구 → no_source_found
    인데 refs 가 있으면 초록(찾았어요)으로 떨어짐.

수정(범위 한정, 0.679·min_score 불변):
 1) matched_evidence 를 확신 근거에서 제외, references 로만 유지.
 2) verdict_hint 우선 - no_source_found 면 refs 있어도 초록 안 감. refs 있으면
    "비슷한 자료는 찾았지만 같은 안내인지는 확인하지 못했어요"(주황 hold).

검증(세 세트):
 - 평가셋 30건: 판정 불변, 25/30 일치, 정상 오판 0
 - 홀드아웃 H01~H05: 불변
 - 실사용 11건: 초록 오출력 2/11(R06 넷플릭스·R11 디지털안내, 둘 다 NIA) → 0/11
 - 라이브 HTTPS: R06·R11 no_source_found 확인 / pytest 79 passed

배포: 프론트(새 번들 index-DuZbGFqH.js) 먼저 → 백엔드 재시작 순.

범위 밖(그대로): 임베딩이 0.679 이상으로 NIA 에 걸리는 경우(예: 접두어 없는
 유사문구 0.6792)는 needs_check→초록 유지. 기관대조/임베딩 FP 별건이라 미수정(지시).
 커밋 6c1d116.
