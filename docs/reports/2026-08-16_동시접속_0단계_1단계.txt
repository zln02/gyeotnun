동시 접속 개선 — 0단계(전체 여정 측정) · 1단계(스레드 1 제한) (2026-08-16, #33)
================================================================================

지시: 단계별로. 각 단계 배포 전 격리 측정 → 배포 → 라이브 확인 → 다음.
      한 번에 합치지 마라.

전부 실측이다. 재현되지 않은 것은 재현되지 않았다고 적었다.
측정은 모두 격리 컨테이너(127.0.0.1:8010)다. 라이브에는 부하를 주지 않았다.


■ 0단계 — 전체 여정 (POST → evidence → dialogue), LLM 실호출 포함
====================================================================

승인된 비용 안에서 돌렸다: **LLM 실호출 13건** (상한 15건, 회당 22.56원 → 약 293원).
스크립트에 예산 상한을 넣어 두었다(bench_journey_client.py LLM_BUDGET).

[0-A] 단독 1명 · 텍스트 여정 (3회)

    POST 0.00s → evidence 0.06~0.10s → dialogue 4.67~5.61s
    dialogue 소요 중앙 4.93s · 전체 여정 최대 5.67s

  ★ 여정 시간의 98%가 질문 생성(LLM)이다. evidence 는 0.1초도 안 된다.

[0-B] 동시 3명 · 텍스트 여정 (2회)

    라운드  사용자  POST끝   evidence끝   dialogue끝   dialogue소요
      1      0     0.00s      0.08s       5.79s        5.70s
      1      1     0.01s      0.26s      10.20s        9.94s
      1      2     0.01s      0.44s      14.77s       14.33s
      2      0     0.00s      0.08s       5.30s        5.22s
      2      1     0.00s      0.32s       9.76s        9.44s
      2      2     0.01s      0.54s      14.51s       13.98s

  ★★ 질문 생성의 동기 호출이 이벤트 루프를 **막는다**. 확인 완료. ★★
    5.8 → 10.2 → 14.8 초. 정확히 4.7초씩 벌어지는 계단이다.
    셋째 사용자는 LLM 이 자기 것을 시작하지도 않은 채 9.5초를 기다린다.
    routers/dialogue.py 의 next_question 은 async def 인데 그 안에서
    anthropic.Anthropic(**동기** 클라이언트)을 부른다. 그동안 워커 전체가 멈춘다.

  ★ 이건 1턴짜리 수치다. 실제 계약은 3턴이므로 동시 3명이면 LLM 호출 9건이
    줄줄이 직렬화된다. 시연에서 세 사람이 동시에 시작하면 마지막 사람의
    마지막 질문까지 40초대가 나온다는 뜻이다(외삽 - 실측 아님).

[0-C] 동시 3명 · 사진 여정 (1회, 실제 사용자 진입 경로)

    사용자  POST끝(OCR)  evidence끝   dialogue끝
      0       4.95s        7.29s      13.27s
      1       4.94s        7.51s      18.79s
      2       7.08s        7.79s      24.14s

  ★ 마지막 사용자가 **24.1초**. OCR 직렬(7초) + LLM 직렬(16초)이 겹친 값이다.

[0 결론] 우선순위 근거

  시연 최대 병목은 **LLM 동기 호출**이다(요청당 4.7초 × 인원 × 턴수).
  OCR(요청당 2.3초)보다 두 배 크다. → 2단계(블로킹 제거)가 효과가 가장 크다.
  1단계(스레드 제한)는 값이 싸고 위험이 없어 먼저 넣지만, 이것만으로는
  여정 시간이 거의 안 줄어든다. 순서를 바꾸지 않는 이유는 아래에 적었다.


■ 1단계 — 모델 스레드 1개로 제한
====================================================================

무엇을 바꿨나: docker-compose.yml api 서비스에 환경변수 4줄.
  OMP_NUM_THREADS · MKL_NUM_THREADS · OPENBLAS_NUM_THREADS · FLAGS_paddle_num_threads = 1
**코드는 한 줄도 안 바꿨다.** 되돌리기는 4줄 삭제 + up -d api.

[1-1] 전후 비교 — 격리 컨테이너, 3회 반복 (동시 1·3·5, 마지막 완료 시각 중앙)

  텍스트 경로 (POST + evidence)
      N |  전(무제한)              후(스레드 1)            실패
      1 |  0.12 / 0.15 / 0.13s     0.09 / 0.12 / 0.13s     0
      3 |  0.62 / 0.55 / 0.51s     0.28 / 0.36 / 0.27s     0
      5 |  1.10 / 0.95 / 0.97s     0.40 / 0.57 / 0.58s     0

      → 동시 5명에서 약 1.8 ~ 2.4배 빨라진다. **느려지지 않는다.**
        (지시가 요구한 확인 항목: 임베딩 경로가 느려지지 않는가 → 아니다, 빨라진다)

  사진 경로 (POST, OCR)
      N |  전(무제한)              후(스레드 1)            실패
      1 |  2.71 / 2.48 / 2.49s     2.69 / 2.21 / 2.20s     0
      3 |  7.00 / 7.40 / 7.17s     7.34 / 7.44 / 6.85s     0
      5 | 11.87 /11.82 /11.81s    12.20 /11.57 /11.46s     0

      → 동시일 때는 **차이 없음(오차 범위)**. 단독 1건만 2.5초 → 2.2초로 빨라진다.

[1-2] ★ 정정 — 앞선 보고의 OCR 수치는 재현되지 않았다

  8/16 첫 측정 보고에 "스레드 1 로 묶으면 동시 5건 11.46s → 8.20s" 라고 썼다.
  다른 하네스(_bench_ocr_client.py)로는 그 값이 재현된다:
      무제한 10.86 / 11.79s   ·   스레드1 8.86 / 9.17s
  그런데 이번 표준 하네스(bench_stage_client.py)로는 차이가 없다(위 표).
  두 하네스의 차이는 워밍업 순서와 측정 반복 횟수뿐이다.

  ★ 왜 갈리는지 설명하지 못한다. 그래서 **OCR 개선은 주장하지 않는다.**
    확실한 것은 두 가지다: 텍스트 경로는 재현성 있게 빨라지고,
    사진 경로는 어느 하네스에서도 **느려지지 않는다.**
    이 두 가지만으로 1단계를 넣을 근거는 충분하다.

  ※ 측정 중 호스트 load average 가 2.4 → 6.0 으로 올랐다. 컨테이너를 계속
    띄웠다 지운 내 측정 자체가 만든 부하다. 절대값은 이 조건에서의 값이다.

[1-3] 배포와 라이브 확인

  docker compose up -d api  (compose 파일 변경이라 restart 가 아니라 up -d)
  컨테이너 안 확인: OMP=1 MKL=1 OPENBLAS=1 PADDLE=1
  prewarm 로그 2줄(임베딩·OCR) 정상

  pytest 172 passed, 2 skipped   ← 판정 로직 무변경 확인

  라이브 확인 (공개 URL, 정상 동작 확인만 — 부하 아님)
      "기초연금 신청 안내입니다"
          hint=needs_check · refs 1 · signals=[official_source_found]
      "정부24 미수령 환급금 확인 gov24-refund-event.com"
          hint=no_source_found · signals=[no_official_source, org_domain_mismatch]
      웹 200

  → 판정이 그대로다. 8/16 에 넣은 기관 도메인 대조도 그대로 뜬다.

[1 결론]

  텍스트 경로 동시 5명이 약 2배 빨라졌다. 메모리 변화 없음(842.8 → 842.5 MiB).
  코드 변경 0. 되돌리기 4줄.
  ★ 다만 사용자 체감 시간은 거의 그대로다. 여정의 98%가 LLM 이기 때문이다(0단계).
    체감을 바꾸는 것은 2단계다.


■ 다음 (2단계 예정)
====================================================================

  collect_evidence · url_expand · LLM 호출을 이벤트 루프 밖으로.
  LLM 은 공식 비동기 클라이언트(anthropic.AsyncAnthropic)가 있으므로 그것을 쓴다.
  ★ 판정 로직은 건드리지 않는다. 실행 방식만 바꾼다.
  ★ render_verdict.mjs 142건 전수 대조로 출력 불변을 확인한 뒤 배포한다.
