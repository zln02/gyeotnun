근거_검증표 경로 차단 · 회귀 검사에 코퍼스 감시 추가 · 하한선 1회 재측정
====================================================================
2026-08-20  (배포 완료)


0. 먼저 — 지시서의 "13개월"은 실제로 20일이었습니다
====================================================

  지시: "레거시 경로가 임베딩 도입 후 13개월(7/28→8/17) 남아 있었다"

  ★ 7/28 → 8/17 은 **20일**입니다. 13개월이 아닙니다. 대장에는 실물대로
    "20일간" 으로 적었습니다. 실물과 지시서가 어긋나면 실물을 따르라는
    8/18 지시 그대로입니다.

  ★ 다만 20일이라고 해서 작은 문제는 아닙니다. 서비스 전체 수명이 그 정도입니다.
    저장소 첫 커밋이 7월 말이니 **거의 전 기간** 남아 있던 셈입니다.


1. match_evidence(근거_검증표) 경로 끄기 — 측정 후 배포
=======================================================

  [1-1] 되돌릴 스위치

    api/services/search.py

        EVIDENCE_TABLE_AS_REFERENCE = False     # 2026-08-19 채택
        ...
        matched_evidence = corpus_index.match_evidence(text) \
                           if EVIDENCE_TABLE_AS_REFERENCE else []

    ★ True 로 바꾸면 즉시 이전 동작으로 복귀합니다. 한 줄입니다.
    ★ corpus/근거_검증표.csv 도 corpus_index.EVIDENCE 적재도 **지우지 않았습니다.**
    ★ 임계값(min_score)을 올리는 방식은 쓰지 않았습니다 — 2로 올리면 매칭이 0이
      되므로(8/17 실측) 사실상 끄는 것과 같은데, 임계값을 만진 기록으로 남습니다.
      끄는 편이 정직하고 되돌리기도 쉽습니다.


  [1-2] 배포 전 측정 (LLM 호출 없음 · 같은 프로세스에서 켜고/끄고 두 번 계산)

    api/experiments/exp_evidence_path_off.py

    ┌────────────────┬───────┬──────────────┬────────────────┐
    │ 대상           │ 무언가 │ ★ hint 변화  │ attention 변화 │
    │                │ 바뀐 건│              │                │
    ├────────────────┼───────┼──────────────┼────────────────┤
    │ 확대 112건     │  10건 │    **0건**   │      3건       │
    │ 홀드아웃 30건  │   4건 │    **0건**   │      0건       │
    │ 실사용 11건    │   3건 │    **0건**   │      3건       │
    └────────────────┴───────┴──────────────┴────────────────┘

    ✓ **verdict_hint 가 바뀐 건이 전 대상에서 0건**입니다.
    ✓ ★★ **사칭 건에서 hint 가 바뀐 것 0건** — 멈춤 조건에 걸리지 않았습니다.

    attention 변화 3건은 `no_official_source` 가 새로 붙는 것입니다.
    ★ 이 키는 ATTENTION_KEYS(허용목록)에 **없습니다.** 그래서 tier 에 닿지 않습니다.
      허용목록이 막는 쪽으로 작동한 사례입니다.
    ★ 재현성 확인: 같은 설정으로 두 번 돌려 결과가 같았습니다.


  [1-3] render_verdict.mjs 142건 전수 대조

    ★ **tier 변화 0건.**

    화면 문구가 바뀐 건 확대112 의 R03 · R05 · R07 **3건뿐**이고 전부 '정상' 입니다.

        제목  "같은 안내인지 확인하지 못했어요"
              → "공식 자료를 못 찾았어요"
        사실  "같은 안내인지는 확인하지 못했어요 · 원문 kostat.go.kr"
              → "공식 자료를 못 찾았어요 · 기관 대표번호로 확인해 보세요"
        링크  kostat.go.kr → (없음)

        tier  hold → hold  (안 움직임)

    ★ 지시에서 "정상 8건이 못 찾았습니다로 바뀌는 것은 정상이다" 라고 하셨는데,
      **실측은 3건**입니다. 8건은 NIA 근거가 붙던 확대셋 건수(8/19 보고 2절)이고,
      그중 5건은 근거가 여러 개라 하나 빠져도 문구가 안 바뀝니다.

    ★ 무엇을 잃었는가 — 정직하게: R03·R05·R07 에서 사라진 통계청 링크는
      "65세 이상 인구 비율" 이었습니다. 청년 상해보험·취업 멘토링 페스타 안내에
      붙던 것이라 **없어지는 게 맞습니다.** 사라진 근거 중 맞는 것은 없었습니다.


  [1-4] 배포

    deploy/verify_restart_race.sh — 통과

        기준 verdict_hint = needs_check (새 프로세스에서 계산)
        재시작 직후 프리워밍 중 동시 5건 → 전부 HTTP 200 · 판정 5/5 일치
        EX-003 증가 0 · EX-006 증가 0 · meta tensor 오류 0
        검사가 만든 checks 6행 자기 정리 (EX 행은 안 지움)

    프론트는 건드리지 않았습니다(백엔드만).

    라이브 컨테이너 확인:

        EVIDENCE_TABLE_AS_REFERENCE = False
        하나카드 광고 → refs 0건 · NIA 없음 · hint=no_source_found

    ★ 8/19 에 제보하신 "자꾸 이 NIA 링크로만 연결된다" 는 라이브에서 끊겼습니다.


  [1-5] ★ 대장에 유형으로 기록 (지시받은 것)

    docs/evaluation/본체결함_발견대장.md 에 "유형" 절을 새로 만들고 L1 로 올렸습니다.

        L1. 새 방식을 넣을 때 옛 방식을 끄지 않으면, 옛 방식이 조용히 오답을 만든다

        7/28 근거_검증표(유일한 대조 장치) → 8/01 하이브리드 → 8/05 임베딩.
        옛 경로는 안 껐다. 그 뒤 20일간 키워드 한 개만 겹쳐도 무관한 통계가 붙었다.

        ★ 8/06 에 절반만 고친 흔적도 함께 적었습니다 — has_confident_source 에서는
          빼면서(주석에 `"이용" → NIA` 를 정확히 적어 뒀습니다) references 와
          신호에는 남겼습니다. **증상을 눌렀고 경로는 남긴 것**입니다.

        규칙: 검색·매칭 방식을 새로 넣을 때 옛 경로를 어떻게 할지 같은 커밋에서 정한다.


2. M3 (점검 주기 없음) — 회귀 검사에 넣었습니다
================================================

  [2-1] 무엇을 넣었나

    api/tools/regression_check.py (매일 05:00 cron)

        official_docs · official_chunks · scam_cases 를 기록하고
        직전 실행과 다르면 **FAIL · 종료코드 1 · "하한선 재측정 필요"**

    기준 파일: /app/data/corpus_size.json

    ★ 자동으로 갱신하지 않습니다. 사람이 재측정한 뒤 직접 고칩니다.
      자동으로 덮으면 경고가 딱 한 번 찍히고 조용히 사라집니다.

    ★ 스킵을 실패로 세는 원칙 그대로, "읽지 못했다"도 실패로 처리합니다.
      코퍼스 import 가 깨져도 초록불이 나오면 안 됩니다.


  [2-2] ★ 실패해야 할 때 실패하는지 확인했습니다

    기준을 1052(8/15 적재 이전 값)로 되돌려 재현:

        [regression] ... FAIL passed=3 failed=0 skipped=0
        [regression] 코퍼스: ★ 코퍼스가 바뀌었다 {'official_docs': '1052->1059'}
                     — **하한선 재측정 필요** ... 재측정: experiments/remeasure_thresholds.py
        종료코드 = 1                         ← 실패

    복원 후 종료코드 = 0.


  [2-3] ★★ 만드는 과정에서 같은 실수를 하루 만에 반복했습니다

    초판에서 기준 파일 경로를 `parents[2]/"deploy"` 로 잡았습니다.
    컨테이너 안에서는 `/deploy` 이고 **마운트되지 않은 경로**입니다.

    → 컨테이너를 다시 만들면 파일이 사라지고, 그러면 매 실행이 "기준 기록 생성"으로
      시작해 **영원히 실패하지 않는 검사**가 됩니다. 아무것도 검사하지 않는 초록불.

    ★ 8/17 에 tools/delete_rows.py 백업 경로에서 똑같은 실수를 했습니다.
      그때 배운 것이 하루 만에 안 옮겨졌습니다. 대장에 L2 유형으로 올렸습니다.

        L2. 컨테이너 안 경로에 쓴 파일은 기본적으로 사라진다
            → /app/data/ 처럼 마운트가 확인된 경로만 쓴다
            → 쓴 뒤에 호스트에서 보이는지 눈으로 확인한다  ← 이번에 이걸로 잡았습니다


  [2-4] ★ 지금 1회 재측정 — 결과만 (조정 안 함)

    api/experiments/remeasure_thresholds.py

    현재 코퍼스: OFFICIAL_DOCS 1,059건 · 청크 2,036개 · SCAM_CASES 51건

    (1) _OFFICIAL_MIN_SCORE = 12.0 (BM25)

        원래 정한 근거:  노이즈 바닥 4.9~10.6  <  12.0  <  진짜 매칭 14.79~31

        지금 실측 — 무관한 문장의 BM25 최고점:

            12.180   오늘 날씨가 좋네요          ← ★ 12.0 을 넘습니다
             9.9xx   저녁에 뭐 먹을지 고민이에요
             ...
            평균 7.793 · 최고 12.180

        진짜 매칭:  N03~N07  19.4 ~ 31.3  통과
                    N01·N02·N08·N10  10.5 ~ 12.0  미달

        ★★ 판정: **위치가 어긋났습니다.**
           "노이즈 최고 < 12.0 < 진짜 최저" 라는 원래 조건이 깨졌습니다.

        ★ 다만 지금 당장 화면이 잘못되지는 않습니다.
          match_official_docs_safe 는 임베딩을 먼저 쓰고, EmbeddingUnavailableError
          가 날 때만 BM25 로 폴백합니다. 즉 **12.0 은 폴백 경로에서만 쓰입니다.**
          실제로 "오늘 날씨가 좋네요" 를 collect_evidence 에 넣으면 refs=0 입니다
          (BM25 단독으로는 1건이 나옵니다).

        ★ 그래서 이건 **잠복 위험**입니다. 임베딩이 실패하는 순간
          — 8/16 에 실제로 겪은 그 경합 — 무관한 문장에 근거가 붙기 시작합니다.
          조용한 열화가 두 겹으로 겹치는 자리입니다.

    (2) match_scam_cases min_score = 5.0 (IDF류)

        _SCAM_N_DOCS = 51 (지금). 경계 구간(3.0~8.0)에 있는 케이스를 셌습니다.
        단어 2개면 대략 9점이라 아직 여유가 있습니다.
        ★ 다만 코퍼스가 커지면 1개로도 넘습니다 (51→500 이면 4.95→7.22).

    ★ 값은 하나도 바꾸지 않았습니다. 조정은 승인 후.


3. 임계값 없는 6종 표 — 대장에 옮겼습니다
==========================================

  L3 유형으로 표를 그대로 옮기고 방어선 칸을 명시했습니다.

      detect_signals        방어선 **없음**
      위험행동              문맥조건
      경보문 판정           사람 검수
      기관 도메인 대조      침묵 규칙 3개
      주소 펼치기           없음
      발행기관 명시         info 신호라 tier 영향 없음

  ★ 지시하신 문장을 그대로 남겼습니다:
    **"없다고 착각하고 어휘만 늘리면 그때 뚫린다."**

  ★ 한 줄 덧붙였습니다 — 방어선이 "없음"인 두 줄이 **어휘 보강(#43)이 실제로 닿는
    곳**입니다. detect_signals 에 단어를 하나 더 넣기 전에 이 표를 먼저 읽습니다.


4. 대장 상태
============

  L1  새 방식 넣고 옛 방식 안 끔          유형 등록 (신규)
  L2  컨테이너 안 경로는 사라진다          유형 등록 (신규 · 내 반복 실수)
  L3  임계값 없는 경로의 방어선           유형 등록 (신규)

  M1  (가) 입력에 없는 것을 전제 13건      미결 — 스팸함 표본 수신 후 재측정
  M2  NIA 통계가 무관한 문자에 붙는다      **해결** (경로 차단 · 배포)
  M3  하한선 재측정 주기 없음              **해결** (회귀 검사 감시 · 자기시험 통과)


5. 검증 요약
============

  pytest             293 passed · 2 skipped
  render_verdict     142건 · tier 변화 0
  exp_evidence_off   112+30+11 · hint 변화 0 · 사칭 변화 0
  회귀 검사 자기시험  코퍼스 불일치 → FAIL · 종료코드 1  (실패해 봤습니다)
  라이브             verify_restart_race.sh 통과 · NIA 링크 끊김 확인

  ★ 지우지 않은 것: 근거_검증표.csv · EVIDENCE 적재 · EX-003 10행 · EX-006 1행 ·
    GN-001 8행 · NIA점유 문서 1부 수치.
