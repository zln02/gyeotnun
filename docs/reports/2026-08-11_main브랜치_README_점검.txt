2026-08-11  main 브랜치 반영 · README 정정 지시 점검 결과
=========================================================

지시받은 6개 항목(1, 2a~2d, 3)을 origin/main 실제 내용과 대조했다.
"미조치"라고 쓰기 전에 최근 커밋에서 처리됐는지 먼저 확인했다.

결론: 5개는 이미 처리돼 있다. 남은 것은 2(d) TODO 목록 정리 하나뿐이다.


0. 전제가 달랐던 부분
---------------------
지시문에는 "main 이 23커밋에서 멈춰 있다"고 적혀 있었으나, 실측값은 다르다.

  origin/main 커밋 수                : 96
  origin/main 최신                   : 1a0a834 Merge pull request #2 from zln02/docs/readme-sync
  origin/main..origin/feat/home-ui   : 0 커밋
  merge-base --is-ancestor 검사       : feat/home-ui 는 main 의 조상 (완전 포함)

즉 feat/home-ui 는 이미 main 에 전부 반영돼 있다. 지시문이 작성된 시점과
현재 사이에 PR #1/#2 가 병합된 것으로 보인다.

로컬/원격 동기 상태도 어긋난 곳이 없다.
  main ... origin/main                   : 0  0
  feat/home-ui ... origin/feat/home-ui   : 0  0


1. feat/home-ui → main 반영
---------------------------
상태: 이미 완료 (조치 불요)

병합은 PR 방식(merge commit)으로 이뤄져 있고, force-push·이력 재작성 흔적은 없다.
충돌 없음. 새로 할 작업이 없다.


2(a). "11. 지금 상태" 이미지 인식 서술
--------------------------------------
상태: 이미 완료

README.md 에 "Claude Vision" 문자열이 존재하지 않는다(git grep 결과 0건).
지시하신 대체 문구가 표 3줄(PaddleOCR / e5-small-ko-v2 / Claude API)까지
그대로 들어가 있다.
반영 커밋: ed23caf "README: 이미지 인식 구버전 서술 정정 + 클론 안내·한계 섹션 추가"


2(b). "8. DB 스키마" corpus 테이블 규모
---------------------------------------
상태: 완료. 단, 지시하신 문자열과 표현이 다르다.

"577" 문자열은 README 에 존재하지 않는다(git grep 0건).

  지시한 문구  : 공식 문서 1,052건 / 검색 청크 2,012개
  현재 문구    : 표 셀은 "공공데이터 문서 (규모는 표 아래 참고)" 이고,
                 표 아래 인용 블록에서
                   - 라이브 인덱스 1,052문서 / 2,012청크 (기획서 평가 기준)
                   - 디스크 재구성본 1,017 / 2,065 는 차기 빌드 예정

수치는 동일하고, 오히려 "라이브 인덱스"와 "디스크 재구성본"을 구분해서
더 정확하다. 임의로 되돌리지 않았다. 지시한 문자열 그대로를 원하시면 말씀해
주시면 바꾼다.


2(c). "새로 클론한 환경에서" 섹션
---------------------------------
상태: 이미 완료

README.md:99 에 지시하신 3개 문단이 토씨까지 그대로 들어가 있다.
설치 안내(배포 섹션) 바로 다음, mock 사용법 앞에 위치한다.


2(d). "11. TODO 목록" 정리
--------------------------
상태: 미조치 — 이것만 남았다.

지시대로 임의 판단 없이, 지울 후보를 먼저 보고한다. 각 항목을 코드로 확인했다.

 항목                              | 판정        | 근거
 ----------------------------------|-------------|------------------------------------
 박진: 링크 본문 추출              | 유지        | api/ 전체에 관련 구현 없음
 박진: 얼굴 블러                   | 유지        | masking.py:354 가 아직 TODO 스텁,
                                   |             | 파일 헤더 15행도 "TODO 스텁" 명시
 김유리: 네이버 검색 API 연동      | 유지(문구↑) | search.py:135 TODO(김유리),
                                   |             | 호출부가 MissingKeyError 로 끝남
 김태희: 프롬프트 튜닝(질문 길이)  | 유지        | 완료 기준이 정의돼 있지 않아 확인 못 함
 장지석: 공공데이터 수집·변환      | ★ 삭제 후보 | api/tools/collect_public_docs.py 존재,
                                   |             | 라이브 인덱스 1,052문서/2,012청크 서빙 중
 장지석: 코퍼스→훈련카드 자동 생성 | 유지        | sample_cards.json 고정 3장뿐.
                                   |             | docs/evaluation/training_card_status.md
                                   |             | 가 "자동 생성 없음"을 명시
 조희진: iOS 대체 경로 실기기 테스트| 유지       | 실기기 테스트 결과 문서를 찾지 못함.
                                   |             | 완료라고 볼 근거가 없어 남긴다
 박진영: 메모리 저장소 → DB 세션   | 유지        | README "알려진 한계" 가 프로세스 메모리
                                   |             | 보관을 현재 상태로 명시
 박진영: Alembic                   | 유지        | db.py:224 TODO 박진영 그대로

명확한 삭제 후보는 **"장지석: 공공데이터 수집·변환" 1건**이다.
같은 줄의 "코퍼스→훈련카드 자동 생성"은 미구현이므로 줄을 통째로 지우면 안 되고,
앞부분만 떼어내야 한다.

추가 제안(승인 필요):
  김유리 항목을 "네이버 검색 API 연동" 그대로 두면, 근거 검색이 아직
  미완인 것처럼 읽힌다. 실제로는 임베딩+BM25 로 근거 검색이 돌고 있고
  네이버는 search.py:14 주석대로 "4) 최신 이슈 보강" 용 선택 레이어다.
  → "김유리: 네이버 검색 API 연동(최신 이슈 보강용 선택 항목)" 로
    한정하는 편이 오해가 적다.


3. "알려진 한계" 섹션
---------------------
상태: 이미 완료

README.md:389 에 지시하신 4개 항목이 그대로 있다(기기 식별자 대조 /
프로세스 메모리 단일 처리 / 질문의 입력 밖 전제 미검사 / 발신자명 인식 누락).


4. 금지 사항 준수
-----------------
force-push · rebase · reset --hard : 실행하지 않음 (읽기 명령만 사용)
코드 수정                          : 없음
docs/security/internal/            : .gitignore:77 에 등재, git ls-files 결과 0건.
                                     추적되지 않으므로 커밋에 들어갈 수 없다


5. 참고 - 워킹트리 미추적 파일
------------------------------
아래는 커밋되지 않은 상태로 남아 있다. 이번 지시 범위 밖이라 손대지 않았다.
  deploy/purge.log
  docs/reports/2026-08-09_임베딩인덱스_커밋여부_판단.txt
  docs/reports/2026-08-09_제공자_기본값_조회.txt
  docs/reports/2026-08-09_카톡_발신자명_재현.txt
