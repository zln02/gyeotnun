"""폴백률 관측의 공용 상수 (2026-08-15).

■ 왜 이 파일이 생겼나
  폴백률을 상시 관측하는 곳이 두 군데다.

    routers/dialogue.py   질문 생성 → 기본 질문 대체    (GN-003)
    services/search.py    임베딩    → BM25 폴백         (EX-006)

  8/13 에 검색 쪽을 붙일 때 "GN-003 과 똑같은 형식으로" 맞추느라 상수 세 개를
  그대로 복사했다. 값이 우연히 같은 게 아니라 **같은 판단을 두 번 적은 것**이다.
  그 상태로는 한쪽만 고쳐도 테스트가 통과하고, 두 관측이 조용히 다른 기준으로
  돌아간다(2026-08-14 감사 §1-2). 그래서 한곳에 둔다.

■ 무엇을 합쳤고 무엇을 안 합쳤나
  합친 것: 창 크기·최소 표본·경보 임계 세 값.
  안 합친 것: 관측 함수 자체. 두 함수는 남기는 코드(GN-003 / EX-006)와 로그
    문구가 다르고, 각자의 모듈 전역 상태(deque·alerted 플래그)를 들고 있다.
    본선 전에 그 상태까지 옮기는 것은 이득보다 위험이 크다. 값만 묶는다.

■ 값의 근거 (2026-08-09, GN-003 을 처음 붙일 때)
  크레딧이 바닥나 8시간 동안 폴백률 100% 였는데 아무도 몰랐다. 사용자 화면에는
  오류가 안 뜨고 질문만 고정 문구가 되는 '조용한 품질 저하'라서다.
  개별 실패는 이미 코드로 남지만 "계속 이러고 있다"를 말해 주는 것이 없었다.
  ★ 한 건의 폴백은 장애가 아니다. 폴백이 일상이 된 것이 장애다.
"""
from __future__ import annotations

# 최근 몇 건을 볼 것인가. 프로세스 메모리(deque)에 이만큼만 들고 본다.
FALLBACK_WINDOW = 20

# 이만큼 쌓이기 전에는 비율을 말하지 않는다.
# ★ 2건 중 2건 폴백(100%)으로 경보하면 재시작 직후마다 시끄러워진다.
FALLBACK_MIN_SAMPLES = 5

# 이 비율을 넘으면 경보. 넘는 동안 한 번만 남기고, 내려오면 다시 무장한다(플래핑 방지).
FALLBACK_ALERT_RATE = 0.5
