# 곁눈(Gyeotnun) API 의존성
# 설치: pip install -r requirements.txt

fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
python-multipart>=0.0.31        # multipart 업로드(이미지) 처리에 필요 (0.0.31: 보안 수정 반영)
SQLAlchemy==2.0.36
psycopg2-binary==2.9.10         # PostgreSQL 드라이버 (docker-compose 용)
httpx==0.28.1                   # 외부 API 호출 (네이버/Vision)
anthropic==0.120.0              # Claude API (질문 생성). 0.42 는 output_config/adaptive thinking 미지원
python-dotenv==1.0.1
rank-bm25==0.2.2                # 공식 문서(OFFICIAL_DOCS) 검색 - BM25Okapi
numpy==2.2.1                     # 임베딩 벡터 연산(코사인 유사도)
# 임베딩 검색(services/embeddings.py) - Upstage Solar Embedding, httpx 로 REST 직접 호출
# (전용 SDK 없이 위 httpx 의존성을 그대로 재사용한다 - 별도 패키지 불필요)

# 로컬 임베딩 검색 (2026-08-04 채택: dragonkue/multilingual-e5-small-ko-v2, 384차원)
# ★ 상용 Upstage API 를 대체한다 - 질의 텍스트가 외부로 나가지 않는다.
#   실측 근거: docs/evaluation/local_embeddings_report.md
#   torch 가 무겁지만(약 1.1GB) CPU 전용 휠을 쓴다 - GPU 라이브러리는 불필요.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.5.1
sentence-transformers==3.3.1

# 로컬 OCR (OCR_PROVIDER=local, 기본). 이미지를 외부로 보내지 않는 오프라인 OCR.
#   구성은 튜닝 실험 baseline(모바일 검출기+문서전처리off+mkldnn on+말풍선필터).
#   모델 캐시는 api/data/paddle_home 에 있다(마운트). OCR_PROVIDER=vision 이면 미사용.
paddlepaddle==3.0.0
paddleocr==3.7.0

# dev
pytest==8.3.4
