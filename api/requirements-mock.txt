# 곁눈(Gyeotnun) — mock 모드 전용 의존성 (2026-08-22)
#
# 설치: pip install -r requirements-mock.txt
# 용도: `?mock=1` 로 화면 흐름만 돌려 보기. 실제 검색·이미지 인식은 하지 않는다.
#
# ★★ 왜 나눴나 — requirements.txt 는 torch 1.1GB 를 받는다 ★★
#   "빠르게 돌려보기" 라고 써 놓고 1.1GB 를 받게 하면 아무도 안 돌린다.
#   실측(2026-08-22): 아래 10개만으로 mock 전체 흐름이 돈다.
#     /health · POST /checks?mock=1 · GET evidence?mock=1 · POST dialogue?mock=1
#
#   ★ 뺄 수 있는 이유는 **지연 import** 다. torch·sentence-transformers 는
#     services/embeddings.py 의 _get_local_model() 안에서, paddle 계열은
#     services/ocr.py 안에서 처음 쓸 때 import 한다. 앱 기동에는 필요 없다.
#   ★ psycopg2-binary 를 뺀 이유: config.py 의 DATABASE_URL 기본값이 sqlite 다.
#     Postgres 로 붙일 때만 필요하다(docker compose 경로).
#   ★ pytest 를 뺀 이유: 실행에 필요 없다. 테스트를 돌리려면 requirements.txt.
#
# ■ 이걸로 못 하는 것 (전부 requirements.txt 가 필요하다)
#     실제 근거 검색(임베딩)   sentence-transformers · torch
#     이미지에서 글자 읽기      paddlepaddle · paddleocr
#     Postgres 연결             psycopg2-binary
#     테스트                    pytest
# ■ mock=0 (실제 경로)로 보내면 — 실측 2026-08-22
#     텍스트 입력    200. 동작한다(OCR 이 필요 없다).
#     근거 검색      200이되 references 0건 — 코퍼스·색인이 저장소에 없기 때문이다.
#                    ★ 오류가 뜨지 않고 근거만 빈다. 조용한 결과이니 오해하지 말 것.
#     이미지 입력    501 (EX-001) — paddleocr 가 없다. "기능이 없다" 는 뜻이다.

fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
python-multipart==0.0.32        # multipart 업로드(이미지) 처리에 필요 (0.0.31: 보안 수정 반영)
SQLAlchemy==2.0.36
httpx==0.28.1                   # 외부 API 호출 (mock 에서는 쓰이지 않지만 import 경로에 있다)
anthropic==0.120.0              # 질문 생성 클라이언트 (mock 에서는 호출하지 않는다)
python-dotenv==1.0.1
rank-bm25==0.2.2                # 코퍼스가 없어도 corpus_index import 에 필요
numpy==2.2.1                    # 임베딩 벡터 연산 (mock 에서는 쓰이지 않지만 import 경로에 있다)
